export function CatIcon({ className }: { className?: string }) {
  return (
    <img
      src="/black-cat.svg"
      alt="DobSpace"
      className={className}
      style={{ objectFit: "contain" }}
    />
  );
}
